{
  "private": true,
  "name": "example",
  "scripts": {
    "start": "node --optimize_for_size --max_old_space_size=920 --gc_interval=100 --use_strict"
  },
  "dependencies": {
  	"express": "latest"
  }
}
