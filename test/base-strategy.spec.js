import test from 'ava-spec';
import strategy from '../lib/templates/base-strategy';

test('templates/baseStrategy should work', t => {
	// const target = {
	// 	output: {
	// 		filename: '[name].js'
	// 	}
	// };
	// t.deepEqual(strategy(undefined, target), target);
});
